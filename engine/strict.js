'use strict'

// strict mode helping to wirte better code by restrcting things
  // throws reference error when variable used without declaring // but in normal mode its allowed.
  // throws reserve word error in case program uses any reserve word -> eg - let interface=10;